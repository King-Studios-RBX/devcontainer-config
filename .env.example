# Copy this file to .env and fill in the values
# NODE_AUTH_TOKEN is required for bun install to pull @king-studios-rbx packages from GitHub Packages
# Get a PAT with read:packages scope from https://github.com/settings/tokens (classic token, not fine-grained)
export NODE_AUTH_TOKEN=""
